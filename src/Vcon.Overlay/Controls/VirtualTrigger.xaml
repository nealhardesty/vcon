<UserControl x:Class="Vcon.Overlay.Controls.VirtualTrigger"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Style="{StaticResource VirtualControlBase}">
    <Grid>
        <Border x:Name="BackgroundBorder"
                Background="{StaticResource ControlBackgroundBrush}"
                BorderBrush="{StaticResource ControlStrokeBrush}"
                BorderThickness="2"
                CornerRadius="8">
            <Grid>
                <Rectangle x:Name="FillIndicator"
                           Fill="{StaticResource XboxGreenBrush}"
                           Opacity="0"
                           RadiusX="6" RadiusY="6"
                           VerticalAlignment="Stretch"
                           HorizontalAlignment="Stretch"
                           Margin="2" />
                <TextBlock x:Name="LabelText"
                           Text="LT"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource ControlTextBrush}"
                           FontWeight="Bold"
                           IsHitTestVisible="False" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
