namespace Vcon.Core.Models;

/// <summary>Vertical edge from which a control's Y offset is measured.</summary>
public enum VerticalAnchor
{
    /// <summary>Y is the distance from the top edge of the screen to the top edge of the control.</summary>
    Top,

    /// <summary>Y is the distance from the bottom edge of the screen to the bottom edge of the control.</summary>
    Bottom,
}
